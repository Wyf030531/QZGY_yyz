将veg100w数据按照规则：“凡属于“2针阔叶混交林”的像元重分类为 2，属于“1针叶林”的重分类为 1，属于“3阔叶林”的重分类为 3，属于“7草丛”“6草原”或“8草甸”的统一合并重分类为 4，属于“4灌丛”的重分类为 5，属于“10高山植被”的重分类为 6，属于“11栽培植被”的重分类为 7，而属于“5荒漠”“9沼泽”以及名称中含“其他”的类型统一重分类为 0；除此之外，所有未被上述条件匹配到的类型也默认归为 0。”进行重分类
对于worldclim的气温降水和短波辐射数据，以：“经度 纬度 一月 二月 ...十二月。然后针对该分辨率下，如30s的降雨数据，按照统一的读取顺序，写入经纬度坐标（像元中心点坐标），然后写入一月的降水数据（一月份的降水tif对应的该点像元值），然后依次遍历。再读取二月的降水数据，继续在该文件内写入二月的降水数据。经纬度，月度值之间用一个空格分隔。”方法对不同分辨率：10m，5m，2.5m，30s的数据进行采样，采样范围为qzgy.shp
